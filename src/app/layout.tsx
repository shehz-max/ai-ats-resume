import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI ATS Resume Optimizer | Get Past Applicant Tracking Systems",
  description: "Optimize your resume for ATS compatibility with AI-powered analysis. Get instant feedback, keyword suggestions, and download ATS-friendly resumes.",
  keywords: ["ATS resume", "resume optimizer", "applicant tracking system", "AI resume", "resume checker"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning
      >
        {children}
      </body>
    </html>
  );
}
